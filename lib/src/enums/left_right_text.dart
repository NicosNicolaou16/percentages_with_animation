/// This enum is using for the right - left text label for linear percentage
enum LeftRightText {
  /// Show only the left text
  leftOnly,

  /// Show only the right text
  rightOnly,

  ///show the left and right text
  both,

  /// disable the left and right text
  none,
}
